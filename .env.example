# --- Database ---
DATABASE_URL=postgresql+psycopg://sentinel:sentinel@localhost:5432/sentinel
REDIS_URL=redis://localhost:6379/0

# --- Telegram notifier ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Frontend ---
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Server-only secret used by the web app's /api/scan route. Set this to the
# same value as the API's CRON_SECRET; never prefix it with NEXT_PUBLIC_.
SENTINEL_CRON_SECRET=
